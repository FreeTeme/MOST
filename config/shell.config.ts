/**
 * Spacing for fixed bottom tab bar. Keep in sync with `AppTabBar` and `--app-tabbar-total` in globals.css.
 */
export const TABBAR_ROW_MIN_PX = 52;
export const TABBAR_PADDING_TOP_PX = 8;
export const TABBAR_PADDING_BOTTOM_MIN_PX = 8;
